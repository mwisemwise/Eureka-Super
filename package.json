{
  "name": "eureka-super",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Capture-first Eureka merge with local encrypted storage, search, optional stats, help, and encrypted export.",
  "license": "MIT",
  "scripts": {
    "dev": "node scripts/dev-server.js",
    "start": "node scripts/dev-server.js",
    "test": "node --check src/js/app.js && node --check src/js/crypto.js && node --check src/js/storage.js && node --check src/js/export.js && node --check scripts/dev-server.js"
  }
}
